export default function Footer() {
  return (
    <footer className="mt-16 border-t py-8 text-center text-xs" style={{ borderColor: 'var(--line)', color: 'var(--ink-soft)' }}>
      <p>Salt &amp; Sundae — a sample menu built with React &amp; Tailwind. Prices shown are illustrative.</p>
    </footer>
  )
}
